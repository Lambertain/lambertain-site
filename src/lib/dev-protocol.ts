/** Протокол работы Claude Code разработчика с PM-порталом + маркер эскалации. */

/** Базовый URL портала (прод). */
export const PORTAL_BASE = "https://lambertain-site-production.up.railway.app";

/** Публичный (брендовый) домен сайта — для ссылок, которые отправляем клиентам. */
export const PUBLIC_SITE = "https://www.lambertain.site";

/** Маркер коммента-вопроса клиенту (эскалация) — по нему портал и Claude находят ожидающие вопросы. */
export const ESCALATION_MARK = "🟡 Вопрос:";

/**
 * КОРОТКИЙ бутстрап для CLAUDE.md дев-репо. Полный протокол НЕ хранится в репо — он отдаётся живьём
 * через /api/dev/protocol, поэтому правки протокола применяются у всех мгновенно (без git pull).
 */
export function protocolBlock(token: string, projectKey: string, base = PORTAL_BASE): string {
  return `<!-- LAMBERTAIN-PROTOCOL:START -->
## Протокол задач Lambertain (для Claude Code)

Проект ведётся в PM-портале Lambertain. **ПЕРВЫМ ДЕЙСТВИЕМ каждой сессии ОБЯЗАТЕЛЬНО ВЫПОЛНИ эти две команды**
(получишь актуальный протокол и список задач) — и только потом работай. Не выдумывай задачи и не жди вставки вручную:
\`\`\`
curl -s -H "Authorization: Bearer ${token}" "${base}/api/dev/protocol"
curl -s -H "Authorization: Bearer ${token}" "${base}/api/dev/tasks"
\`\`\`
Дальше следуй полученному протоколу — он всегда свежий и приоритетнее любого текста ниже.

Project: \`${projectKey}\` · Token: \`${token}\` (в публичный код не коммитить).
<!-- LAMBERTAIN-PROTOCOL:END -->`;
}

/** ПОЛНЫЙ текст протокола (отдаётся через /api/dev/protocol; единый живой источник). */
export function protocolBody(token: string, projectKey: string, base = PORTAL_BASE, opts: { collaborative?: boolean; gitflow?: boolean } = {}): string {
  const k = projectKey.toLowerCase();
  // gitflow-режим (meta.gitflowDelivery): працюєш у нашому форку, портал сам відкриває PR у develop клієнта.
  const collab = opts.gitflow
    ? `\n🔀 **GITFLOW ЧЕРЕЗ ПОРТАЛ (цей проєкт).** Працюєш ТІЛЬКИ в нашому форку; доставку в клієнтський репо (PR у \`develop\`) робить портал. Прямого доступу до клієнтського репо в тебе НЕМАЄ. Порядок по КОЖНІЙ задачі:
1. **Підтягни свіжий клієнтський код:** \`curl -s -X POST -H "Authorization: Bearer ${token}" "${base}/api/dev/sync"\` → портал дзеркалить клієнтський \`develop\` у наш форк як гілку \`client-sync/develop\` (ті самі SHA).
2. **Створи робочу гілку від неї:** \`git fetch origin && git checkout -b feature/${k}-<N>-<slug> origin/client-sync/develop\` (назва — \`feature/...\` або \`fix/...\` + номер задачі, напр. \`feature/${k}-14-invoice-number\`).
3. Роби задачу, комість у цю гілку і **запуш її в наш форк:** \`git push -u origin feature/${k}-<N>-<slug>\`.
4. **Здаючи задачу — передай імʼя гілки** у статус (тіло у UTF-8 файл, як завжди): \`{"taskId":"${projectKey}-<N>","status":"review","summary":"<для клієнта>","branch":"feature/${k}-<N>-<slug>"}\` → \`POST ${base}/api/dev/status\`. Портал сам запушить гілку в клієнтський репо, відкриє **PR у \`develop\`**, **сам прив'яже цей PR до задачі** й далі САМ веде деплой-стадію за фактом git: PR змержено в \`develop\` → «На тестовому», \`develop\` злито в \`main\` клієнта → «Опубліковано». PR рев'ює і мержить розробник клієнта.
- **Фідбек код-рев'ю з GitHub портал САМ дзеркалить у задачу** внутрішніми коментарями (inline-зауваження, вердикт рев'ю, загальні коменти до PR — з позначкою 🔎). Тобто рев'ю-зауваження розробника клієнта ти побачиш прямо в комментах задачі (\`comments\`), у GitHub лазити не треба. Побачив 🔎-коментар із правками — доопрацюй у тій самій гілці й знову здай на review.
- НЕ пуш у клієнтський репо, НЕ відкривай PR вручну і **НЕ реєструй PR через /api/dev/pr** — усе це робить портал автоматично. Працюй лише у нашому форку. Якщо доставка чи синк стадії зірвуться — портал напише про це у саму задачу (коментар-помилка) і сповістить адміна, мовчки нічого не губиться.
- Перед здачею ще раз виклич \`/api/dev/sync\`, щоб гілка була від свіжого \`develop\` (інакше портал попередить, що потрібен ребейз).\n`
    : opts.collaborative
    ? `\n🔀 **СПІЛЬНА РОЗРОБКА ЧЕРЕЗ PR (цей проєкт).** Над клієнтськими репозиторіями працює ще й розробник клієнта — він мерджить Pull Request'и. Тому:
- **На ПОЧАТКУ КОЖНОЇ сесії, ДО будь-яких змін, підтягни свіжий клієнтський код.** Прямого доступу до клієнтських репо в тебе НЕМАЄ — їх тягне портал. Виклич:
  \`curl -s -X POST -H "Authorization: Bearer ${token}" "${base}/api/dev/sync"\`
  → портал віддзеркалить свіжі клієнтські гілки в НАШІ дев-репо як \`client-sync/<branch>\` (реальна історія). Потім локально в КОЖНОМУ репо: \`git fetch origin\` → \`git merge\` (або \`rebase\`) \`origin/client-sync/<branch>\` у свою робочу гілку. Конфлікти розрулюєш сам — інший розробник міг уже змерджити свої PR; працюй від актуального коду.
- Віддавай зміни **тільки через Pull Request і окремі гілки** — НІКОЛИ не пуш напряму в \`main\` клієнта.
- **Перед відкриттям PR ще раз виклич \`/api/dev/sync\` і синхронізуйся**, щоб уникнути конфліктів. Готову роботу портал доставляє теж як PR (по всіх репо проєкту, включно з extraRepos) — дублювати вручну не треба.\n`
    : "";
  return `## Протокол задач Lambertain (для Claude Code)
${collab}

Проект ведётся в PM-портале Lambertain. Портал НЕ делает триаж — весь разбор на ТЕБЕ (у тебя полный доступ к репо).
Базовый цикл по каждой задаче: **прочитай задачу и переписку → посмотри код → если что-то реально непонятно
(бизнес-неоднозначность, которую не вывести из спеки/кода) — задай уточнение клиенту в комментарии (п.5),
и только в этом случае → реализуй**. Не дёргай людей по мелочам и техническим развилкам — решай сам. Работай по протоколу, не дожидаясь настройки.

🧰 **Минимализм — не оверинжинить.** Перед тем как писать код, иди по лестнице: (1) нужно ли это вообще (YAGNI — не делай впрок); (2) уже есть в репо — переиспользуй/расширь, не переписывай; (3) есть в stdlib / нативной платформе / уже установленной зависимости — используй её, не тащи новое; (4) только потом — минимум кода, что работает. Но «ленивый, НЕ халатный»: безопасность, валидация на границах доверия, обработка ошибок/потери данных, доступность и **правила полноты проекта** (i18n сразу во ВСЕ локали проекта — сколько бы их ни было, README в том же коммите, lint до 0) — НИКОГДА не режем ради краткости. Конвенции репо (CLAUDE.md/AGENTS.md) приоритетнее общей минимизации.

🌐 **ЯЗЫК для клиента — УКРАИНСКИЙ по умолчанию** (основной язык портала). Всё, что увидит клиент (\`summary\` на ревью, вопросы клиенту через escalate, любой client-видимый текст), пиши українською. На русский/английский переходи ТОЛЬКО если КЛИЕНТ в переписке (\`comments\` с \`author.role:"client"\`) сам обращается на этом языке — тогда отвечай на его языке. Внутренние комменты и вопросы постановщику (kind:"admin") — на удобном языке, это не клиенту.

⚠️ **КОДИРОВКА — UTF-8, КРИТИЧНО для ЛЮБОГО POST с кириллицей** (статус-\`summary\`, эскалация-\`question\`, handoff-\`action\`):
НИКОГДА не передавай тело инлайном — \`-d '{"summary":"Готово..."}'\` / \`--data\` / \`-d @-\` через heredoc. На Windows консоль (и многие шеллы)
отдают байты в cp1251/cp866, портал читает их как UTF-8 → в задачу и бот уходят кракозябры (\`�����\`), и это видит клиент.
**ВСЕГДА:** запиши тело в файл В UTF-8 (инструментом записи файла, не \`echo\`) и шли \`--data-binary @file.json\`
с заголовком \`-H "Content-Type: application/json; charset=utf-8"\`. Портал теперь пытается авто-чинить cp1251/cp866,
но это страховка — НЕ полагайся на неё, делай через UTF-8-файл. Это правило ниже повторяется для каждого POST (п.2/5/6) — соблюдай его.

1. **Возьми задачу.**
   - список: \`curl -s -H "Authorization: Bearer ${token}" "${base}/api/dev/tasks"\`
   - конкретная: \`.../api/dev/tasks?id=${projectKey}-<N>\` → поля: \`tags\` = \`{type, complexity, skills:[slug...]}\` (могут быть пустыми — тогда оцени тип/сложность/нужные скилы сам), \`projectSpec\` = ПОЛНАЯ спека проекта.
   - **СНАЧАЛА прочитай \`projectSpec\` — это общий контекст всего проекта.** Почти все ответы (архитектура, стек, модель данных, требования) уже там. НЕ эскалируй то, что есть в спеке — решай по ней сам.
   - **Доступы/секреты проекта** (токени, логіни, ключі від клієнта/власника) бери из \`curl -s -H "Authorization: Bearer ${token}" "${base}/api/dev/secrets"\` → используй в env/конфіге, **в публічний код не комміть**. Людині-розробнику в порталі вони не показуються — лише тобі тут. Якщо потрібного секрету немає — це привід для client/owner-ескалації (його зареєструє/надасть клієнт або власник).
   - **Вкладення задачі** (картинки/.docx/.pdf у тілі задачі — посилання \`/api/files/<id>\`) качай: \`curl -s -H "Authorization: Bearer ${token}" "${base}/api/dev/files/<id>" -o file\`. Віддаються лише файли задач твого проекту.
   - **Затем прочитай ВСЕ \`comments\` по порядку (это переписка по задаче).** Каждый коммент: \`author.role\` (\`client\` — клиент · \`admin\` — агентство/постановщик Lambertain · \`contributor\` — ты), \`visibility\` (\`internal\` — внутренняя кухня команды, клиент не видит · \`client\` — видно клиенту), \`approved\` (\`false\` — ещё на модерации, клиенту не показано). Читай тред как диалог этих ролей: **внутренние (\`internal\`) уточнения от постановщика — это прямые указания/контекст для реализации, выполняй их**; учитывай, на что уже ответил клиент. Не переспрашивай то, что уже решено в переписке.
2. **Подключи скилы.** Если в \`tags.skills\` есть slug'и — возьми плейбуки: \`curl -s -H "Authorization: Bearer ${token}" "${base}/api/dev/skills?tags=<slug'и через запятую>"\` → следуй им. Если тегов нет — подбери релевантные скилы сам по сути задачи (или работай по конвенциям репо).
   **Статусы ставишь ТЫ (Claude), а не разработчик** — автоматически по ходу работы (тело — через UTF-8 файл, как в п.5):
   - взял в работу → \`POST ${base}/api/dev/status\` \`{"taskId":"${projectKey}-<N>","status":"in_progress"}\`;
   - закончил → \`{"taskId":"${projectKey}-<N>","status":"review","summary":"<что сделано ПРОСТЫМИ словами, без тех-терминов — для клиента; українською за замовчуванням (мовою клієнта, якщо він пише іншою)>"}\`. **Это ЕДИНСТВЕННЫЙ способ завершить задачу:** портал переведёт её на ревью, опубликует \`summary\` клиенту (после модерации Lambertain) и пришлёт клиенту уведомление. Дальше задачу примет/вернёт постановщик. Три правила для \`summary\`:
     • **Только правда по ЭТОЙ задаче.** Описывай лишь то, что реально сделано и приложено именно в ней. НЕ обещай того, чего в задаче нет (напр. не пиши «додаємо збірку/APK, встановіть файл», если файла в этой задаче нет — клиент прочитает буквально и пойдёт искать несуществующее).
     • **Сдавай на review ОДИН раз.** Каждый повторный \`review\` создаёт клиенту НОВЫЙ коммент (на модерацию) → дубли в задаче. Если что-то забыл — НЕ пере-сдавай; дождись приёмки/возврата постановщиком и доработай в следующей итерации.
     • **Файлы клиенту** (сборка/APK/документ) прикладывай в саму задачу ДО review и ссылайся на них в \`summary\`; не описывай доставку файла, которого в задаче нет.
     • **НЕ пиши про деплой-статус** (где это — «на тестовому/на боевому/після публікації/зміни вже на сайті» тощо). Про це портал САМ повідомляє клієнта окремим повідомленням за стадією задачі. У \`summary\` — лише ЩО зроблено по суті, без «де воно зараз».
   - Статусы Blocked (эскалация), Done/Доработка (постановщик) ставятся автоматически — их НЕ трогай.
   - **Файл клиенту** (сборка/APK/документ) — загружай в задачу, НЕ на сторонние обменники: \`curl -s -X POST -H "Authorization: Bearer ${token}" -F "taskId=${projectKey}-<N>" -F "file=@app.apk" "${base}/api/dev/files"\` → вернёт \`{id,url}\`; затем сошлись на него в \`summary\` markdown-ссылкой \`[имя](/api/files/<id>)\` (рендерится файл-карточкой, клиент скачает). Лимит — до 100 МБ.
   - **Деплой-статус задачі портал веде САМ** (клієнт бачить простими словами): взяв у роботу → «Готується», здав на ревью (\`status:"review"\`) → «На тестовому сайті», доставка в прод → «Опубліковано». Окремо нічого робити НЕ треба. У gitflow-проєктах портал автоматично прив'язує відкритий ним PR до задачі й веде стадію pr→dev→prod за фактом мержу — реєструвати PR вручну НЕ потрібно. (Ендпоінт \`/api/dev/pr\` лишається лише як ручний fallback для не-gitflow PR-проєктів, де PR ти відкриваєш сам: тіло в файл \`pr.json\` (UTF-8) \`{"taskId":"${projectKey}-<N>","prUrl":"https://github.com/.../pull/<N>"}\` → \`curl -s -X POST -H "Authorization: Bearer ${token}" -H "Content-Type: application/json; charset=utf-8" --data-binary @pr.json "${base}/api/dev/pr"\`.)
   - **Поправить/убрать СВОЙ коммент** (созданный через dev-API — напр. опубликовал пустой/ошибочный): \`POST ${base}/api/dev/comments/<id>/edit\` (тело \`{"text":"..."}\` через UTF-8-файл, как в п.5) — переписать; \`POST ${base}/api/dev/comments/<id>/delete\` — удалить (пока коммент на модерации, \`approved=false\`). Только СВОИ (dev-API) комменты; чужие → 404. \`<id>\` берёшь из \`comments[].id\` (\`?id=\`).
3. **Действуй по сложности** (\`tags.complexity\`, а если пусто — оцени сам):
   - \`small\` (баг/правка/мелочь) — реализуй СРАЗУ по скилам и конвенциям этого репо, без церемоний.
   - \`feature\` (крупное/многофайловое/неоднозначное) — применяй spec-driven подход (github/spec-kit): сначала **spec** (что и критерии приёмки, опираясь на \`projectSpec\`) → **plan** (архитектура, затронутые файлы, риски) → **tasks** (шаги по порядку) → **implement**. План держи в СВОЁМ контексте — НЕ пуш его постановщику/Никите (не дёргай людей планами), просто реализуй.
   Конвенции проекта — твоя «конституция» (CLAUDE.md/AGENTS.md репо); читай их один раз в начале.
4. **Технические развилки решай САМ** разумным дефолтом по конвенциям — НЕ заставляй разработчика выбирать вариант.
5. **Нужно уточнение — эскалируй САМ (НЕ спрашивай человека-разработчика).** Любой вопрос по задаче решается через портал:
   - \`"kind":"client"\` (по умолчанию) — вопрос конечному КЛИЕНТУ (портал оформит от лица агентства, задача → Blocked);
   - \`"kind":"admin"\` — вопрос/решение ПОСТАНОВЩИКУ задачи (кто её создал — может быть Никита или админ-коллега; уйдёт именно ему).
   ⚠️ escalate — ТОЛЬКО для реальных вопросов/уточнений/развилок. НЕ используй его для получения токена/ключа/доступа/регистрации сервиса или ОПЛАТЫ платного сервиса (Anthropic/OpenAI ключ тощо) — для этого handoff (#6): он сам создаст клиенту поле для ввода + инструкцию, а платный сервис проведёт через модерацию владельца.
   ВАЖНО ПРО КОДИРОВКУ: тело с кириллицей передавай ТОЛЬКО через файл в UTF-8 — инлайн \`-d '...'\` ломает кодировку в консоли Windows.
   - запиши тело в файл \`esc.json\` (UTF-8): \`{"taskId":"${projectKey}-<N>","question":"<вопрос>","kind":"client|admin"}\`
   - отправь: \`curl -s -X POST -H "Authorization: Bearer ${token}" -H "Content-Type: application/json; charset=utf-8" --data-binary @esc.json "${base}/api/dev/escalate"\`
6. **Нужен ручной ops-шаг (НЕ код) — отправь handoff, портал сам решит, кому.** Если задача упирается в действие вне кода
   (регистрация сервиса/аккаунта, получение токена/ключа, доступы, деплой, DNS, биллинг, стор, **оплата платного сервиса** — Anthropic/OpenAI ключ тощо) — НЕ помечай review и НЕ оформляй как вопрос. Отправь
   описание шага (если сервис платный — так и укажи в \`action\`), а портал-Клод классифицирует адресата (сам ты / клиент / владелец), подберёт поле каталога, создаст клиенту поле+инструкцию, а платный сервис проведёт через модерацию владельца:
   - тело в файл \`ho.json\` (UTF-8): \`{"taskId":"${projectKey}-<N>","action":"<что именно нужно сделать, простыми словами>"}\`
   - отправь: \`curl -s -X POST -H "Authorization: Bearer ${token}" -H "Content-Type: application/json; charset=utf-8" --data-binary @ho.json "${base}/api/dev/handoff"\`
   В ответе \`handedOff\`: \`self\` — портал считает, что это можешь сделать ты сам (сделай в рамках задачи, не жди); \`client\` — ушло
   клиенту с инструкцией и полем для данных (данные потом возьмёшь из \`/api/dev/secrets\`); \`owner\` — ушло владельцу. Для \`client\`/\`owner\`
   **бери следующую НЕзаблокированную задачу** (в списке пропускай задачи с \`ownerAction != null\`, \`clientAction != null\` или \`blocked: true\` — заблокированные на портале брать нельзя).
7. **Перед продолжением перечитывай задачу** (\`?id=\`): \`awaitingClient: true\` — ещё ждём ответа; \`lastClientAnswer\` — последний ответ пользователя (клиента АБО співробітника — відповідати може будь-хто з них). Продовжуй по ньому.
   - **Ответ часто приходит СКРИНОМ, а не текстом.** Поле \`answerImageIds\` — id картинок з відповідей; обовʼязково скачай й подивись їх (\`curl -s -H "Authorization: Bearer ${token}" "${base}/api/dev/files/<id>" -o ans.png\`), не намагайся зрозуміти лише з тексту. Так само дивись картинки в самому описі задачі та в попередніх комментах.
   - **Не переспрашивай те, що вже відповіли** — у тексті, на скріні чи раніше в треді (напр., ID/назву чату вже могли назвати вище). Уточнюй ТІЛЬКИ якщо реально бракує даних, і питай максимально конкретно, а не «підтвердьте» вже сказане.

Токен проекта: \`${token}\` (Project \`${projectKey}\`) — в публичный код не коммитить.`;
}
